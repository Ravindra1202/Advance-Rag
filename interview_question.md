# document loader 
# text splitters

# embedding

1. Indexing:
    1. HNSW(Hierarchical Navigable Small Worlds )
        HNSW (Hierarchical Navigable Small Worlds) is a highly popular, state-of-the-art graph indexing algorithm used in vector databases to perform lightning-fast Approximate Nearest Neighbor (ANN) searches in high-dimensional spaces.
        - Small Worlds-> reduce the no. of search (search the sub-set of enbedding)
        - Navigable--> fow the path to navigate( path/root ) the best search
        - Hierarchical->>
        