# Vector store:
persist of embedding that is store the embedding in database


# vector strore in indexing
Indexing is the process of synchronising your raw data sources (like PDFs, databases, or web pages) with a Vector Store, ensuring that your data is efficiently chunked, embedded, and kept up to date without duplicates